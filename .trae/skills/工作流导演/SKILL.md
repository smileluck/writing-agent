---
name: workflow-producer
description: |
  [MASTER ENTRY POINT] 写作工作流总导演 - 所有写作请求的唯一入口点。
  触发词：帮我写、写文章、写一篇、创作、写作、产出、起草
---

# ⚠️ 此文件为跳转桩，唯一源文件位于：

**`.kilo/skill/工作流导演/SKILL.md`**

请使用 `read` 工具直接读取上述路径获取最新指令。
禁止使用 `skill` 工具加载（存在缓存风险）。
